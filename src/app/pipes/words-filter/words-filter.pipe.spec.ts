import { WordsFilterPipe } from './words-filter.pipe';

describe('WordsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new WordsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
